[package]
name = "fusion_plus"
version = "1.0.0"
upgrade_policy = "compatible"

[addresses]
fusion_plus = "0x0e6067afa8c1ca1b0cc486ec2a33ef65c3d8678b67ce9f1e4995fddae63cd25b"

[dev-addresses]
fusion_plus = "0x234"

[dependencies]
AptosFramework = { git = "https://github.com/aptos-labs/aptos-core.git", subdir = "aptos-move/framework/aptos-framework", rev = "main" }

[dev-dependencies]
AptosFramework = { git = "https://github.com/aptos-labs/aptos-core.git", subdir = "aptos-move/framework/aptos-framework", rev = "main" }

[profile.default]
# Enable all linters
all_features = true
# Enable all warnings
warnings = true
# Enable all checks
check = true
# Enable all tests
test = true
# Enable all documentation
doc = true
# Enable all formatting
fmt = true
# Enable all verification
verify = true

[profile.release]
# Optimize for size
optimize = true
# Enable all linters
all_features = true
# Enable all warnings
warnings = true
# Enable all checks
check = true
# Enable all tests
test = true
# Enable all documentation
doc = true
# Enable all formatting
fmt = true
# Enable all verification
verify = true

[profile.test]
# Enable all linters
all_features = true
# Enable all warnings
warnings = true
# Enable all checks
check = true
# Enable all tests
test = true
# Enable all documentation
doc = true
# Enable all formatting
fmt = true
# Enable all verification
verify = true